// Enkel grund för SHBF Beer App

document.addEventListener('DOMContentLoaded', function() {
  // Exempel: visa en alert när sidan laddas
  // alert('Välkommen till SHBF Beer App!');

  // Sätt fokus på sökrutan
  var searchInput = document.getElementById('searchInput');
  if (searchInput) {
    searchInput.focus();
  }

  // Lägg till fler funktioner här efter behov
});
